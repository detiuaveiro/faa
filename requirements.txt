# Acceleration: NumPy (system build with OpenBLAS preferred) and JAX
numpy>=2.0.0
scipy>=1.13.0
jax>=0.4.30

# Machine learning: scikit-learn for classic ML, Keras 3 on the JAX backend (export KERAS_BACKEND=jax)
scikit-learn>=1.5.0
keras>=3.0.0

# Parallelization
joblib>=1.4.0

# Data handling (polars, never pandas) and plotting
polars>=1.0.0
matplotlib>=3.9.0
seaborn>=0.13.0
tqdm>=4.66.0

# Group libraries
pyBlindOpt>=0.5.0
emptyspacesearch>=0.7.0
kneeliverse>=1.2.0
pyUTSAlgorithms>=0.2.0
pyNNMF>=0.2.0

# Notebooks (presentation and labs)
jupyter>=1.0.0
ipykernel>=6.29.0
